import * as schedulingActions from "./actions";
import * as schedulingSchemas from "./schemas";

export const schedulingCore = {
  buildGeneratedSlots: schedulingActions.buildGeneratedSlots,
};

export { schedulingActions, schedulingSchemas };
